package sample;

import javafx.scene.image.Image;

public class PlayerBullet extends Bullet {
    public PlayerBullet(int x, int y, int w, int h, Image image, int damage) {
        super(x, y, w, h, image, damage);
    }
}
